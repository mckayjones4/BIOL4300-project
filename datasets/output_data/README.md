# Folder Organization
Each folder contains the raw output files from IQ-TREE (`*_MaximumLikelihood`) and BEAST (`*_Bayesian`).

The trait_metadata folder contains output .csv files from the script (`datasets/code/06.trait_analysis.R`)
